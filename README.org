An Emacs plugin for the [[https://lean-lang.org][Lean 4]] theorem prover, working
toward feature parity with [[https://github.com/Julian/lean.nvim][lean.nvim]].

Built on built-in *Eglot*.

#+begin_quote
⚠️ *Vibe-coded.* This project is written largely by prompting an AI coding
agent. Expect rough edges; review before relying on it.
#+end_quote

* Status

Early. The current milestone is the RPC keystone: connect to the server and show
the live goal. Unicode abbreviation input (\to → →, \alpha → α) is also in.

| Command            | Binding   | Evil (Doom) | Description                                    |
|--------------------+-----------+-------------+------------------------------------------------|
| ~leanmacs-goal~    | ~C-c C-g~ | ~SPC m g~   | Show the interactive goal at point.            |
| ~leanmacs-input-activate~ | --  | --          | Turn on the Leanmacs Unicode input method.     |

The =Evil (Doom)= binding is the suggested local-leader key; see [[*Keybindings][Keybindings]] to
enable it.

* Unicode input

~leanmacs-mode~ activates a Quail input method named =Leanmacs=. Type a
backslash followed by an abbreviation to insert the corresponding Unicode
symbol:

| Type      | Inserts | Note                          |
|-----------+---------+-------------------------------|
| ~\alpha~  | α       |                               |
| ~\to~     | →       |                               |
| ~\and~    | ∧       |                               |
| ~\<>~     | ⟨⟩      | point lands between the pair  |
| ~\norm~   | ‖‖      | point lands between the pair  |
| ~\\~      | \       | escape the leader             |

The ~$CURSOR~-style abbreviations (mostly matching delimiter pairs) expand with
point placed where the cursor marker was, which lean4-mode's input method does
not do.

Toggle the method off and on in a buffer with ~toggle-input-method~. To stop
~leanmacs-mode~ from enabling it automatically, set ~leanmacs-input-enable~ to
~nil~. The table is generated from ~data/abbreviations.json~; after changing
that file, run ~leanmacs-input-setup~ to rebuild.

* Requirements

- Emacs 29.1+ (developed on 30.2).
- A Lean 4 toolchain (~lake~, ~lean~) on ~PATH~, e.g. via
  [[https://github.com/leanprover/elan][elan]].

* Installation

#+begin_src elisp
(add-to-list 'load-path "/path/to/lean-emacs")
(require 'leanmacs-mode)
#+end_src

Open a ~.lean~ file in a Lake project, start the server with ~eglot~, then run
~leanmacs-goal~ inside a proof. The goal buffer then follows the cursor.

* Development

Run the batch test suite (pure functions, rendering, RPC wire-format, the input
method -- no Lean server needed):

#+begin_src sh
make test
#+end_src

~make compile~ byte-compiles every source file, and ~make clean~ removes the
artifacts. Override the Emacs binary with e.g. ~make EMACS=emacs-30.1 test~.
CI runs these on several Emacs versions via GitHub Actions (see
~.github/workflows/ci.yml~).

The end-to-end test in ~test/e2e.el~ talks to a real ~lake serve~ and is run
manually, not by ~make test~; see the file header for instructions.

* License and credits

MIT (see ~LICENSE~). Behavior and the RPC handling are ported from the MIT
~lean.nvim~ by Julian Berman. ~nael~ and ~lean4-mode~ were design references
only (no code copied).

The one exception is ~data/abbreviations.json~, which comes from the VS Code
Lean 4 extension ([[https://github.com/leanprover/vscode-lean4][leanprover/vscode-lean4]]) and is licensed under Apache-2.0; see
~NOTICE~, ~data/README.org~, and ~LICENSES/Apache-2.0.txt~.
